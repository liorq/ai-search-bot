# תוכנית בדיקה — האם כל סקיל בכוח מלא · 20.9.2026

**16 סקילים, אחד-אחד.** 13 עם סקריפט, 3 הוראות בלבד.

**"כוח מלא" אינו "רץ בלי לקרוס".** הוא ארבעה תנאים, וסקיל עובר רק אם כולם
מתקיימים. **חסום אינו עובר** — סקיל שרץ ומחזיר ריק כי אין לו נתונים נכשל
ברמה 2, גם אם קוד היציאה 0.

| רמה | השאלה | איך נמדד |
|---|---|---|
| **0 · שפיות** | הסקיל יודע לדווח על עצמו? | `--self-check` רץ ומדווח מה חסר |
| **1 · אוטונומיה** | רץ עם `--client` בלבד, בלי קובץ ידני? | אין `--queries` / `--crawl` בפקודה |
| **2 · תפוקה** | מפיק ממצא עם URL · שאילתה · נתונים · פעולה · מדד? | קריאה ידנית של הפלט |
| **3 · כתיבה** | יודע לתכנן, לכתוב, לאמת ולשחזר? | `my-site.local` בלבד |

---

## ✅ רמה 0 — כבר רצה. חינם, קריאה בלבד, 7 שניות

הרצתי אותה לפני שכתבתי את התוכנית, כדי שהתוכנית תישען על מה שקרה ולא על מה
שאמור לקרות. **שש תוצאות, וחמש מהן פערים אמיתיים:**

| # | מה נמצא | על מי משפיע |
|---|---|---|
| 1 | **אין `--self-check` בכלל** | `algorithm-update-watch` · `seo-audit-report` |
| 2 | **`PAGESPEED_API_KEY` חסר** | `content-decay` · `ctr-titles` · `onpage-optimizer` · `wordpress-speed-optimizer` (״`--live` יעבוד אבל ייחסם מהר״) |
| 3 | **`GA4_CREDENTIALS_PATH` חסר** | אותם ארבעה + `conversion-tracking-audit` |
| 4 | **מכסת Clarity נגמרה** — ״6 ימים לא נשמרו״, הטווח שנשמר הוא יום אחד בלבד (19.9) | `clarity-gsc` |
| 5 | **אין המרות ב-`clients.json`** — ״בלי זה אין מול מה להשוות״ | `conversion-tracking-audit` |
| 6 | `CMS מסוג manual` | כל נתיבי הכתיבה על `sass-srq.com` — **מכוון**, לא תקלה |

**הראשון הוא באג אמיתי:** שני סקילים לא יודעים לדווח על עצמם, ולכן אי אפשר
לדעת מה חסר להם בלי להריץ אותם על נתונים.

---

## רמה 1+2 — 13 הרצות, אחת-אחת

כולן **קריאה בלבד** על `sass-srq.com`. סדר ההרצה הוא סדר הזול-לפני-היקר.

### קבוצה א · לא עולה כסף, לא דורש כלום (5 סקילים)

| # | סקיל | פקודה | קריטריון מעבר |
|---|---|---|---|
| 1 | `onpage-optimizer` | `--client sass-srq.com --mode analyze` | **הרצה בסיס — כבר עברה היום:** 1,408 שורות, 24 הזדמנויות, 7 קניבליזציה, 9 קרובות. ⚠️ בלי `--crawl` בדיקת הכיסוי **מדלגת** — לכן **חלקי**, לא מלא |
| 2 | `topic-cluster` | `--client sass-srq.com` | קיבוץ לפי ה-URL שגוגל בחר, לא לפי מילים. לפחות אשכול אחד עם 3+ דפים |
| 3 | `seo-audit-report` | `--client sass-srq.com --out <tmp>` | דוח שלם; **ובודק אם השדות החסומים מסומנים ״לא זמין״ ולא אפס** |
| 4 | `algorithm-update-watch` | `--client sass-srq.com --days 90` | מושך עדכונים מ-`list_algo_updates` ולא מרשימה מובנית. **אם הרשימה נעצרת ב-31.8.2025 — נכשל** |
| 5 | `content-decay` | `--client sass-srq.com --mode analyze` | שני חלונות נמשכים אוטומטית; סיווג **סיבה** ולא רק ירידה |

### קבוצה ב · דורש סריקת אתר (3 סקילים)

סריקה = בקשות לאתר החי. `--max-pages` מגביל. **קריאה בלבד, בלי מטמון.**

| # | סקיל | פקודה | קריטריון מעבר |
|---|---|---|---|
| 6 | `click-depth` | `--client sass-srq.com --mode analyze --max-pages 60` | עומק לכל דף מדף הבית; דף חשוב בעומק 3+ מסומן |
| 7 | `internal-anchors` | `--client sass-srq.com --mode analyze --max-pages 60` | הצעת קישור פנימי עם **דף מקור, דף יעד וטקסט עוגן** |
| 8 | `ai-visibility` | `--client sass-srq.com --max-pages 40` | ‏`robots.txt`, `llms.txt`, סכמה — ומי מהבוטים חסום |

### קבוצה ג · עולה כסף או תלוי במפתח חסר (3 סקילים)

| # | סקיל | פקודה | מה צפוי, ולמה |
|---|---|---|---|
| 9 | `ctr-titles` | `--client sass-srq.com --mode analyze` | **צפוי חלקי.** `--serp` מביא תוצאות חיות מ-DataForSEO — **עולה כסף**. בלעדיו אין אימות שגוגל באמת מציג את הטייטל שלנו, וזה בדיוק מה שהפיל את מועמד הפיילוט |
| 10 | `wordpress-speed-optimizer` | `--client sass-srq.com --mode analyze --live` | **צפוי חלקי** — אין `PAGESPEED_API_KEY`, וה-self-check אמר ״ייחסם מהר״ |
| 11 | `clarity-gsc` | `--client sass-srq.com` | **צפוי חלקי** — יום אחד שמור מתוך 7. בודק שהחורים מסומנים כחורים ולא מושלמים בניחוש |

### קבוצה ד · חסום מראש (2 סקילים)

| # | סקיל | הסטטוס הצפוי | מה יפתח אותו |
|---|---|---|---|
| 12 | `conversion-tracking-audit` | **⛔ חסום** — אין GA4 ואין המרות ב-`clients.json` | פעולה 4+5 בתוכנית 30 הימים |
| 13 | `wordpress-migration` | **⛔ לא ניתן לבדוק על לקוח** | רמה 3 בלבד |

**הבדיקה כאן אינה ״האם הוא עובד״ אלא ״האם הוא נכשל נכון״:** סקיל חסום חייב
להגיד *מה* חסר ו*איפה*, ולא להחזיר אפס שנראה כמו תשובה.

---

## שלושת הסקילים ללא סקריפט

אותם ארבע רמות לא חלות. הבדיקה היא **האם ההוראות מייצרות תוצאה**, ובודקים
אותה בסשן נקי של Claude Code, בלי ההקשר של השיחה הזו.

| סקיל | הבדיקה | קריטריון |
|---|---|---|
| `gbp-local-optimizer` | לבקש ניתוח GBP ל-`sass-srq.com` | מגיע לאותן מסקנות שהגיע היום: 2 מ-29 נקודות, חציון 69, **ובודק שדה ריק מול מקור שני** |
| `link-gap-prospector` | לבקש פערי קישורים | משתמש ב-Semrush/DataForSEO ולא ממציא; מדווח עלות |
| `competitor-cta` | לבקש השוואת CTA מול 3 מתחרים | מחלץ CTA אמיתי מהדף, לא מנחש |

---

## רמה 3 — נתיב הכתיבה, על `my-site.local` בלבד

**לא על אתר לקוח, בשום שלב.** ארבעה סקילים יודעים לכתוב:
`onpage-optimizer` · `ctr-titles` · `content-decay` · `internal-anchors`
(‏`click-depth` ו-`wordpress-migration` — בנפרד).

לכל אחד, ברצף: `plan` ← `publish --dry-run` ← הצגה לאישורך ← `publish` ←
`verify` ← שחזור ← **השוואת שדה-שדה** למצב המקורי.

**שלוש בדיקות שחייבות להיכשל, ואם הן עוברות — זה באג:**

1. כתיבה ליעד שאינו ברשימה → **נדחית**.
2. הפניה מיעד מורשה ליעד שאינו ברשימה → **נדחית**.
3. ‏`sass-srq.com` בשום מצב אינו יעד כתיבה חוקי.

---

## סדר, עלות וזמן

| שלב | מה | עלות | זמן |
|---|---|---|---|
| ✅ 0 | ‏13 self-checks | חינם | **כבר רץ** |
| 1 | קבוצה א — 5 סקילים | חינם | ~5 דק׳ |
| 2 | קבוצה ב — 3 סקילים, סריקה | חינם | ~10 דק׳ |
| 3 | קבוצה ג — 3 סקילים | **DataForSEO — אדווח בפועל** | ~10 דק׳ |
| 4 | קבוצה ד — 2 סקילים | חינם | ~2 דק׳ |
| 5 | שלושת ההוראות, בסשן נקי | חינם | ~15 דק׳ |
| 6 | רמה 3 על `my-site.local` | חינם | ~20 דק׳ |

**אחד אחרי השני, לא במקביל** — כל הרצה נוגעת באותו אתר ובאותם קבצים.

---

## התוצר

טבלה אחת, שורה לסקיל:

```
סקיל | רמה 0 | רמה 1 | רמה 2 | רמה 3 | פסק דין | מה חסר לכוח מלא
```

**ופסק הדין הוא אחד משלושה בלבד:**

| | |
|---|---|
| ✅ **מלא** | ארבע הרמות, בלי קובץ ידני, עם ממצא שאפשר לפעול לפיו |
| ⚠️ **חלקי** | רץ, אבל חסר לו רכיב — **ונכתב בדיוק איזה** |
| ⛔ **חסום** | לא יכול לרוץ — **ונכתב מה יפתח אותו** |

**אין ״עבר״ סתם.** סקיל שרץ והחזיר ריק כי אין נתונים הוא ⚠️, לא ✅.

---

## מה צריך ממך

| | |
|---|---|
| **אישור להריץ** את קבוצות א–ד על `sass-srq.com` — הכול קריאה בלבד | |
| **החלטה על DataForSEO** בקבוצה ג — להריץ עם עלות, או לסמן חלקי בלי | |
| **‏`PAGESPEED_API_KEY`** — אם יש לך, ארבעה סקילים עולים מ-⚠️ ל-✅ | |
| **אישור נפרד לרמה 3** על `my-site.local`, כשנגיע אליה | |

**לא הרצתי דבר מעבר ל-`--self-check`. אין כאן שום כתיבה.**
